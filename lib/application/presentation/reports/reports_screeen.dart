import 'package:flutter/material.dart';

class ReportsScreeen extends StatelessWidget {
  const ReportsScreeen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Reports"),
      ),
    );
  }
}
